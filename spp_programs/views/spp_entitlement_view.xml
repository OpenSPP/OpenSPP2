<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- TODO: Create base views or fix inheritance -->
    <!-- Temporarily commented out to allow module loading -->
    <!--
    <record id="inherit_view_entitlement_tree" model="ir.ui.view">
        <field name="name">inherit_view_entitlement_tree</field>
        <field name="model">spp.entitlement</field>
        <field name="priority">1</field>
        <field name="inherit_id" ref="spp_programs.view_entitlement_tree" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='valid_until']" position="after">
                <field name="currency_id" column_invisible="1" />
                <field name="initial_amount" sum="total_initial_amount" />
            </xpath>
            <xpath expr="//list" position="attributes">
                <attribute name="delete">0</attribute>
            </xpath>
        </field>
    </record>

    <record id="inherit_view_entitlement_form" model="ir.ui.view">
        <field name="name">inherit_view_entitlement_form</field>
        <field name="model">spp.entitlement</field>
        <field name="priority">1</field>
        <field name="inherit_id" ref="spp_programs.view_entitlement_form" />
        <field name="arch" type="xml">
            <xpath expr="//button[@name='approve_entitlement']" position="attributes">
                <attribute
                    name="groups"
                >spp_security.group_spp_admin,spp_programs.group_programs_manager,spp_programs.group_programs_validator</attribute>
            </xpath>
            <xpath expr="//button[@name='approve_entitlement']" position="after">
                <button
                    type="object"
                    class="btn-danger"
                    icon="fa-thumbs-o-down"
                    name="reject_entitlement"
                    title="Reject entitlement"
                    invisible="state not in ('draft','pending_validation')"
                    string="Reject Entitlement"
                    groups="spp_security.group_spp_admin,spp_programs.group_programs_manager,spp_programs.group_programs_rejector"
                />
                <button
                    type="object"
                    class="btn-primary"
                    icon="fa-repeat"
                    name="reset_to_pending"
                    title="Reset to Pending"
                    invisible="state != 'reject'"
                    string="Reset to Pending"
                    groups="spp_security.group_spp_admin,spp_programs.group_programs_manager"
                />
            </xpath>
            <xpath expr="//field[@name='date_approved']" position="after">
                <field name="date_rejected" readonly="1" invisible="not date_rejected" />
                <field name="rejected_reason" readonly="1" invisible="not rejected_reason" />
            </xpath>
        </field>
    </record>
    -->

    <!-- Odoo 19: Cannot modify menu groups via record, must use menuitem groups attribute
<record id="spp_programs.menu_entitlement" model="ir.ui.menu">
        <field name="groups" eval="[Command.link(ref('spp_programs.read_program_cycle_entitlement'))]" />
    </record>
-->

    <!-- Odoo 19: Fixed field name from groups_id to group_ids -->
    <record model="ir.actions.server" id="reject_entitlement_modal">
        <field name="name">Reject</field>
        <field name="model_id" ref="spp_programs.model_spp_reject_entitlement_wizard" />
        <field name="binding_model_id" ref="spp_programs.model_spp_entitlement" />
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="code">action = model.open_wizard()</field>
        <field
            name="group_ids"
            eval="[Command.link(ref('spp_security.group_spp_admin')), Command.link(ref('spp_programs.group_programs_manager')), Command.link(ref('spp_programs.group_programs_validator')), Command.link(ref('spp_programs.group_programs_cycle_approver')), Command.link(ref('spp_programs.group_programs_rejector'))]"
        />
    </record>

    <!-- Odoo 19: Fixed field name from groups_id to group_ids -->
    <!-- Note: Commented out as this record may not exist or needs full definition -->
    <!--
    <record id="spp_programs.multiple_entitlement_approval" model="ir.actions.server">
        <field name="name">Multiple Entitlement Approval</field>
        <field
            name="group_ids"
            eval="[Command.link(ref('spp_security.group_spp_admin')), Command.link(ref('spp_programs.group_programs_manager'))]"
        />
    </record>
    -->

    <!-- Odoo 19: Fixed field name from groups_id to group_ids -->
    <record model="ir.actions.server" id="reset_to_pending_entitlement_modal">
        <field name="name">Reset to Pending</field>
        <field
            name="model_id"
            ref="spp_programs.model_spp_reset_pending_entitlement_wizard"
        />
        <field name="binding_model_id" ref="spp_programs.model_spp_entitlement" />
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="code">action = model.open_wizard()</field>
        <field
            name="group_ids"
            eval="[Command.link(ref('spp_security.group_spp_admin')), Command.link(ref('spp_programs.group_programs_manager')), Command.link(ref('spp_programs.group_programs_validator')), Command.link(ref('spp_programs.group_programs_cycle_approver'))]"
        />
    </record>
</odoo>
