<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!--
        Concept Groups are now created via post_init_hook in __init__.py
        using search-or-create pattern for idempotent installation/upgrade.

        This file is kept for documentation purposes.

        Standard concept groups created:
        - feminine_gender (is_female)
        - masculine_gender (is_male)
        - head_of_household (is_head)
        - pregnant_eligible (is_pregnant)
        - climate_hazards
        - geophysical_hazards
        - children
        - adults
        - elderly
        - persons_with_disability

        To add codes to a concept group, use the UI or Python code:

        group = env['spp.vocabulary.concept.group'].search(
            [('name', '=', 'feminine_gender')], limit=1
        )
        group.write({'code_ids': [Command.link(your_female_code.id)]})

        ADR-016 Phase 3: CEL Integration
    -->
</odoo>
