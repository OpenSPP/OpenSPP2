<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!-- Exit Registrant Detail Form View -->
    <record id="spp_cr_detail_exit_registrant_form" model="ir.ui.view">
        <field name="name">spp.cr.detail.exit_registrant.form</field>
        <field name="model">spp.cr.detail.exit_registrant</field>
        <field name="arch" type="xml">
            <form string="Exit Registrant Details" readonly="not is_cr_manager">
                <header>
                    <field name="is_cr_manager" invisible="1" />
                    <button
                        name="action_next_documents"
                        string="Next: Upload Documents"
                        type="object"
                        class="btn-primary"
                        icon="fa-arrow-right"
                        invisible="approval_state not in ('draft', 'revision')"
                        groups="spp_change_request_v2.group_cr_manager"
                    />
                    <button
                        name="action_save_and_go_to_list"
                        string="Save as Draft"
                        type="object"
                        class="btn-outline-secondary"
                        invisible="approval_state not in ('draft', 'revision')"
                        groups="spp_change_request_v2.group_cr_manager"
                    />
                    <field
                        name="stage"
                        widget="statusbar"
                        statusbar_visible="details,documents,review"
                    />
                </header>
                <sheet>
                    <group>
                        <group string="Registrant">
                            <field name="registrant_name" />
                            <field name="is_group" invisible="1" />
                            <field
                                name="active_membership_count"
                                invisible="is_group"
                            />
                        </group>
                        <group string="Exit Details">
                            <field name="exit_reason" />
                            <field name="exit_date" />
                        </group>
                    </group>
                    <group>
                        <group
                            string="Additional Details"
                            invisible="exit_reason not in ['deceased', 'emigrated']"
                        >
                            <field
                                name="death_date"
                                invisible="exit_reason != 'deceased'"
                                required="exit_reason == 'deceased'"
                            />
                            <field
                                name="destination_country_id"
                                invisible="exit_reason != 'emigrated'"
                                options="{'no_create': True, 'no_open': True}"
                            />
                        </group>
                        <group string="Options">
                            <field name="is_end_active_memberships" />
                        </group>
                    </group>
                    <group string="Additional Information">
                        <field
                            name="remarks"
                            placeholder="Enter any additional notes..."
                        />
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>
