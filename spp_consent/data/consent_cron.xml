<?xml version="1.0" encoding="utf-8" ?>
<!--
Part of OpenSPP. See LICENSE file for full copyright and licensing details.

Scheduled actions for consent management.
-->
<odoo noupdate="1">
    <!-- Cron job to check for expired consents -->
    <record id="ir_cron_check_expired_consents" model="ir.cron">
        <field name="name">Consent: Check for Expired Consents</field>
        <field name="model_id" ref="spp_consent.model_spp_consent" />
        <field name="state">code</field>
        <field name="code">model._cron_check_expired()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="active">1</field>
    </record>
</odoo>
