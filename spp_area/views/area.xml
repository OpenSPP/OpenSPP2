<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <!-- Add g2p registrar and g2p admin group in Area Menu -->
    <!-- Odoo 19: Cannot modify menu groups via record, must use menuitem groups attribute
<record id="spp_area.menu_spparea" model="ir.ui.menu">
        <field
            name="groups"
            eval="[Command.link(ref('spp_registry.group_registry_officer')), Command.link(ref('spp_security.group_spp_admin'))]"
        />
    </record>
-->

</odoo>
